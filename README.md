# HK-Task5-Automated-Bulk-Email-Sender
A robust, CLI-based Automated Bulk Email Sender built with Python. Features include dynamic personalization (using {name} and {company} placeholders), CSV/JSON contact loading, secure SMTP login, automatic retry mechanism for failed emails, transaction logging (email_history.json), and bonus support for file attachments and HTML templates.
